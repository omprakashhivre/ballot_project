import React from 'react';
import './styles.scss';
import { Options } from '../types';
declare type Props = {
    message: string;
    resolver: (decision: boolean) => void;
    options?: Options;
};
declare const ConfirmBox: React.FC<Props>;
export default ConfirmBox;
